{
  "name": "logic-grid-solver",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "parcel serve src/index.html",
    "build": "parcel build src/index.html",
    "predeploy": "rimraf dist && parcel build src/index.html --public-url https://alaggydev.github.io/logic-grid-solver",
    "deploy": "gh-pages -d dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "rimraf": "^5.0.5"
  }
}
